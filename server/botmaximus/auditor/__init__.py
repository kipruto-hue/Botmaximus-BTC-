"""The Auditor — read-only observability (Auditor Master Prompt v1.0).

Reads the ledgers, writes reports, touches nothing. Its power is entirely
borrowed from the ledgers: it can say nothing that isn't already in them, and it
can do nothing at all.
"""
